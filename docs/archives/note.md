- 搁置未修复的BUG：
  - 1.窗口在“非最大化”状态时，光标悬停在“最大化按钮”上不弹出 Snap Layout
  - 2.dark 模式下， 一般状态窗口为纯白色，最大化后窗口变为黑色（未正确应用 dark 模式背景色）
  - 3.window effect 未能表现预期效果，一般状态窗口为纯白色（不透明），最大化后会发现背景颜色稍微变化（色差不大的浅白色）
  - 4.resize 窗口大小时，整体会有轻微抖动（之前遇到的闪烁问题大概率是因为加入文字后导致的渲染问题，因为把内容区清空就看不到了）
  - 5. window effect 的半透明效果在某些情况下会失效，最大化后再还原，窗口的半透明效果失效，唤起其他应用窗口置于其上，然后再切回本窗口，半透明效果才会恢复

2026.3.2
window effect 可以显示半透明效果，但是存在 bug ：
 - 程序刚启动时没有效果 目前已修复
 - 最大化后再还原，窗口的半透明效果失效，唤起其他应用窗口置于其上，然后再切回本窗口，半透明效果才会恢复，目前原因未知；但是偶然发现，在失效时之前所遇到的bug1会出现，但是失效前bug1就不会出现了，说明这两个问题可能是相关的，目前正在调查中。