name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release
  # 6.9.2+ drops the AGL.framework link (QTBUG-137687), which Apple removed
  # from the macOS 26 / Xcode 26 SDK used by the macos-latest runner. The last
  # 6.8 patch (6.8.3) still links AGL, so it cannot build on that runner.
  QT_VERSION: '6.9.3'

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        build_type: [Release]
        c_compiler: [gcc, clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            qt_arch: win64_msvc2022_64
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
            qt_arch: linux_gcc_64
          - os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++
            qt_arch: linux_gcc_64
          - os: macos-latest
            c_compiler: clang
            cpp_compiler: clang++
            qt_arch: clang_64
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl
          - os: macos-latest
            c_compiler: gcc
          - os: macos-latest
            c_compiler: cl

    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update && sudo apt-get install -y \
          build-essential libgl1-mesa-dev libxcb-xinerama0 \
          libx11-xcb1 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
          libxcb-randr0 libxcb-render-util0 libxcb-shape0 \
          libxcb-xfixes0 libxkbcommon-x11-0 libfontconfig1 libfreetype6

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ env.QT_VERSION }}
        arch: ${{ matrix.qt_arch }}
        cache: true

    - name: Configure CMake
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -S ${{ github.workspace }}

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: ctest --build-config ${{ matrix.build_type }} --output-on-failure
      env:
        # The utils regression test inspects the native GWL_STYLE, which needs
        # the real windows platform. Windows runners have a desktop; headless
        # platforms (offscreen) are used on Linux/macOS.
        QT_QPA_PLATFORM: ${{ runner.os == 'Windows' && 'windows' || 'offscreen' }}
